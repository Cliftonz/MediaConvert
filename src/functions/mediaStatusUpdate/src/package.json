{
  "name": "repo-analysis",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npx pnpm i && esbuild app.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=app.js",
    "playwright:test": "playwright test test/"
  },
  "dependencies": {
    "@octokit/auth-app": "^4.0.9",
    "@octokit/rest": "^19.0.7",
    "@prisma/client": "^4.12.0",
    "@sentry/nextjs": "^7.47.0",
    "@upstash/kafka": "^1.3.3",
    "@upstash/qstash": "^0.3.6",
    "langchain": "^0.1.33",
    "axios": "^0.24.0",
    "simple-git": "^3.17.0",
    "superjson": "^1.12.2",
    "weaviate-ts-client": "^1.4.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.114",
    "esbuild": "^0.17.18"
  }
}
